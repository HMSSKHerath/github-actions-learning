name: Conditional Execution

on:
  push:
    branches: [main, develop]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run deployment (main only)
        if: github.ref == 'refs/heads/main'
        run: echo "Deploying to production..."
      